# Legacy Artifacts

- `OUT_literal/` stores files originally written under a literal `$OUT/` directory.
- This directory is kept for traceability and local debugging, and is ignored by git.
- Avoid creating literal `$OUT` paths; prefer explicit output paths under `output/` or `repro/.../runs/`.
